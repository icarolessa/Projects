// Solicitando um numero pelo navegador

// o prompt só funciona no navegador
let primeiroNumero = prompt("Digite um número: ");

// imprimindo número recebido pelo usuário
console.log(primeiroNumero)